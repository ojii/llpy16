import dev.display
import static

def squared(num):
   num *= num
   return num

A = squared(2)
A = dev.display.num_to_char(A)
A = dev.display.prepare(A, static.color_white, static.color_black)
dev.display.write(A, 0)
